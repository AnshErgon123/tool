   # Flask secret key for session management and security
   SECRET_TOKEN=supersecret
   
   # Flask environment
   FLASK_ENV=development
   FLASK_APP=main.py
   
   # CAN interface configuration (if needed)
   CAN_INTERFACE=can0
   CAN_BITRATE=500000
   
   # WebSocket configuration
   SOCKETIO_CORS_ALLOWED_ORIGINS=*